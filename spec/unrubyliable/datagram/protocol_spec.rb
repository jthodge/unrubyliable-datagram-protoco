RSpec.describe Unrubyliable::Datagram::Protocol do
  it "has a version number" do
    expect(Unrubyliable::Datagram::Protocol::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
